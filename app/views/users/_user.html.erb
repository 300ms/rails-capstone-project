<li class="user_info">
  <h1> <%= image_tag user.image.url if user.image? %> </h1>
  <h3> <%= link_to user.name, user_path(user) %></h3>
  <h5> <%= user.email %> </h5>
  <p> <%= user.created_at %> </p>
</li>
