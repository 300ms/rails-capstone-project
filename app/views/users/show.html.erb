<% provide(:title, @user.name) %>
<%= link_to "Home", root_path %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1> <%= @user.image %> </h1>
      <h3> <%= @user.email %></h3>
    </section>
  </aside>
</div>
